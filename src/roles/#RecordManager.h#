#include<vector>
#include<string>
#include"models/table.h"
#include<map>

using namespace std;

class RecordManager {
public:
	bool createTable(string tableName);   //create file: *.table
	bool dropTable(string tableName);  //delete file: *.table
	vector<TableRowPtr> select(string tableName);
	vector<TableRowPtr> select(string tableName, vector<Condition> conditions);
//	vector<TableRowPtr> select(vector<Address> addresses);
	Address insert(string tableName, vector<String> data);
	int delete(string tableName, vector<Condition> conditions);
	int delete(vector<Address> addresses); 
}
